/* Fill this in with your own Firebase project's config to turn sign-in on.
 * Firebase console → Project settings → General → Your apps → add a Web app
 * → "SDK setup and configuration" shows exactly this object — copy it here
 * as-is.
 *
 * These values identify your project; they are not secret credentials and
 * are safe to commit to a public repo. Firebase's own docs are explicit
 * about this — access to your data is controlled by Firebase Security
 * Rules, not by hiding this file. Do not paste a service-account key here;
 * that is a different, genuinely secret thing this file is not for.
 *
 * Until you replace PLACEHOLDER below, the app runs with sign-in disabled
 * and skips straight to the map, exactly as it did before accounts existed.
 */
window.WHEREABOUTS_FIREBASE_CONFIG = {
  apiKey: "AIzaSyDykoAsKyiM0xfPPDk00KyPhF7Oh3jpfiE",
  authDomain: "whereuare.firebaseapp.com",
  projectId: "whereuare",
  appId: "1:1006185976450:web:277ccf6220b4d80b969a9b"
};
